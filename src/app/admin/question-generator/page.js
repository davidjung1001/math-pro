import QuestionGeneratorTool from '@/components/admin/QuestionGeneratorTool';

export const metadata = {
  title: 'AI Question Generator — Admin',
};

export default function QuestionGeneratorPage() {
  return <QuestionGeneratorTool />;
}
